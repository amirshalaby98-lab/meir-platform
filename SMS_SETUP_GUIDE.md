# دليل تفعيل نظام SMS - موقع مير

هذا الدليل يشرح كيفية تفعيل نظام الإشعارات عبر SMS في موقع مير.

## 📱 نظرة عامة

نظام SMS في موقع مير يرسل إشعارات تلقائية للعملاء في الحالات التالية:
- ✅ **تأكيد الحجز**: عند إنشاء حجز جديد (يتضمن رقم الحجز)
- 👨‍🔧 **تخصيص الفني**: عند تعيين فني للحجز (يتضمن اسم الفني ورقمه)
- 🎉 **إتمام الخدمة**: عند إكمال الحجز (يتضمن رابط التقييم)
- ❌ **إلغاء الحجز**: عند إلغاء الحجز

---

## 🔧 الخطوات المطلوبة

### 1. اختيار مزود SMS

الموقع يدعم 3 مزودين رئيسيين:

#### أ) **Twilio** (عالمي - الأفضل للدول العربية)
- الموقع: https://www.twilio.com
- المميزات: موثوق، سهل الاستخدام، دعم ممتاز
- التكلفة: ~$0.0079 للرسالة الواحدة
- مناسب لـ: جميع الدول

#### ب) **Unifonic** (متخصص في السعودية والخليج)
- الموقع: https://www.unifonic.com
- المميزات: متخصص في السوق السعودي، أسعار تنافسية
- التكلفة: ~0.03 ريال للرسالة
- مناسب لـ: السعودية، الإمارات، الكويت

#### ج) **SMS Misr** (متخصص في مصر)
- الموقع: https://smsmisr.com
- المميزات: أسعار منخفضة، خدمة محلية
- التكلفة: ~0.05 جنيه للرسالة
- مناسب لـ: مصر فقط

---

### 2. إنشاء حساب والحصول على API Keys

#### لـ Twilio:
1. سجل في https://www.twilio.com/try-twilio
2. احصل على رصيد تجريبي مجاني ($15)
3. من Dashboard، احصل على:
   - `Account SID`
   - `Auth Token`
   - `Phone Number` (رقم الإرسال)

#### لـ Unifonic:
1. سجل في https://www.unifonic.com
2. من لوحة التحكم، احصل على:
   - `App SID` (API Key)
   - `Sender ID` (اسم المرسل - يجب أن يكون معتمد)

#### لـ SMS Misr:
1. سجل في https://smsmisr.com
2. من لوحة التحكم، احصل على:
   - `Username`
   - `Password`
   - `Sender Name` (اسم المرسل)

---

### 3. إضافة API Keys في الموقع

1. افتح واجهة إدارة المشروع في Manus
2. اذهب إلى **Settings → Secrets**
3. أضف المتغيرات حسب المزود المختار:

#### لـ Twilio:
```
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_PHONE_NUMBER=+966501234567
```

#### لـ Unifonic:
```
UNIFONIC_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
UNIFONIC_SENDER_ID=Meir
```

#### لـ SMS Misr:
```
SMS_MISR_USERNAME=your_username
SMS_MISR_PASSWORD=your_password
SMS_MISR_SENDER=Meir
```

---

### 4. تفعيل الكود في الملف

1. افتح ملف `/server/sms.ts`
2. ابحث عن القسم الخاص بالمزود المختار
3. **ألغِ التعليق** عن الكود (احذف `//` من بداية كل سطر)

#### مثال لـ Twilio:

**قبل:**
```typescript
// === TWILIO INTEGRATION ===
// const accountSid = process.env.TWILIO_ACCOUNT_SID;
// const authToken = process.env.TWILIO_AUTH_TOKEN;
// const fromNumber = process.env.TWILIO_PHONE_NUMBER;
// 
// const client = require('twilio')(accountSid, authToken);
// await client.messages.create({
//   body: message,
//   from: fromNumber,
//   to: `+${to}`
// });
```

**بعد:**
```typescript
// === TWILIO INTEGRATION ===
const accountSid = process.env.TWILIO_ACCOUNT_SID;
const authToken = process.env.TWILIO_AUTH_TOKEN;
const fromNumber = process.env.TWILIO_PHONE_NUMBER;

const client = require('twilio')(accountSid, authToken);
await client.messages.create({
  body: message,
  from: fromNumber,
  to: `+${to}`
});
```

5. **احذف أو علّق** كود Mock (السطور 33-36):
```typescript
// Mock SMS sending (logs to console)
// console.log("📱 SMS Notification:");
// console.log(`To: ${to}`);
// console.log(`Message: ${message}`);
// console.log("---");
```

---

### 5. تثبيت المكتبات المطلوبة

حسب المزود المختار، قم بتثبيت المكتبة:

#### لـ Twilio:
```bash
pnpm add twilio
```

#### لـ Unifonic أو SMS Misr:
لا حاجة لتثبيت مكتبات إضافية (يستخدم fetch المدمج)

---

### 6. الاختبار

1. قم بإنشاء حجز تجريبي من الموقع
2. تحقق من:
   - وصول رسالة SMS إلى رقم الهاتف
   - ظهور الرسالة في Console (إذا كنت تستخدم Mock)
   - عدم وجود أخطاء في logs

3. اختبر جميع السيناريوهات:
   - إنشاء حجز → يجب أن تصل رسالة تأكيد
   - تخصيص فني → يجب أن تصل رسالة بمعلومات الفني
   - إتمام الحجز → يجب أن تصل رسالة مع رابط التقييم
   - إلغاء الحجز → يجب أن تصل رسالة الإلغاء

---

## 🔍 استكشاف الأخطاء

### المشكلة: الرسائل لا تصل

**الحلول:**
1. تأكد من صحة API Keys في Settings → Secrets
2. تأكد من إلغاء التعليق عن الكود الصحيح
3. تأكد من تثبيت المكتبة المطلوبة
4. تحقق من رصيد الحساب في لوحة تحكم المزود
5. تحقق من أن رقم الهاتف بالصيغة الصحيحة (966XXXXXXXXX)

### المشكلة: خطأ في الكود

**الحلول:**
1. تأكد من عدم وجود أخطاء في Console
2. تأكد من أن جميع المتغيرات معرّفة
3. أعد تشغيل الخادم بعد التعديلات

### المشكلة: الرسائل تصل متأخرة

**الحلول:**
1. هذا طبيعي، بعض المزودين يأخذون 1-5 ثواني
2. تأكد من استقرار الإنترنت
3. جرب مزود آخر

---

## 💰 التكاليف المتوقعة

### حسابات تقريبية:

**سيناريو: 100 حجز شهرياً**
- 100 رسالة تأكيد حجز
- 80 رسالة تخصيص فني (80%)
- 70 رسالة إتمام (70%)
- 10 رسائل إلغاء (10%)
- **المجموع: 260 رسالة/شهر**

**التكلفة الشهرية:**
- Twilio: 260 × $0.0079 = **$2.05** (~8 ريال)
- Unifonic: 260 × 0.03 = **8 ريال**
- SMS Misr: 260 × 0.05 = **13 جنيه**

---

## 📝 ملاحظات مهمة

1. **الرسائل التجريبية**: معظم المزودين يوفرون رصيد تجريبي مجاني
2. **أرقام الاختبار**: في وضع التجربة، قد تحتاج لتسجيل أرقام الاختبار
3. **Sender ID**: بعض الدول تتطلب تسجيل اسم المرسل رسمياً
4. **الامتثال**: تأكد من الامتثال لقوانين الرسائل النصية في بلدك
5. **الخصوصية**: لا تشارك API Keys مع أحد

---

## 🚀 نصائح للتحسين

1. **راقب الاستخدام**: تابع عدد الرسائل المرسلة شهرياً
2. **اختبر أولاً**: استخدم أرقام تجريبية قبل الإطلاق
3. **احتفظ بنسخة احتياطية**: احفظ API Keys في مكان آمن
4. **راجع الأسعار**: قارن الأسعار بين المزودين دورياً
5. **استخدم Templates**: بعض المزودين يوفرون قوالب معتمدة

---

## 📞 الدعم الفني

إذا واجهت أي مشاكل:
1. راجع هذا الدليل أولاً
2. تحقق من documentation المزود
3. تواصل مع دعم المزود مباشرة
4. راجع ملف `/server/sms.ts` للتفاصيل التقنية

---

**آخر تحديث:** ديسمبر 2025
**الإصدار:** 1.0
